# Mobile Terminal Environment Configuration

# Server port (default: 3456)
PORT=3456

# Password for login (default: terminal123)
TERMINAL_PASSWORD=your-secure-password-here

# Node environment (development or production)
NODE_ENV=development

# Shell to use (default: /bin/zsh on macOS)
# SHELL=/bin/zsh
